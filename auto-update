#!/bin/bash

function update
{
  apt-get update &&
  apt-get dist-upgrade -Vy &&
  apt-get autoremove -y &&
  apt-get autoclean &&
  apt-get clean

  if [ $1 = '-r' ]; then
    shutdown -r now
  elif [ $1 = '-e' ]; then
    exit
  fi
}

update $1
